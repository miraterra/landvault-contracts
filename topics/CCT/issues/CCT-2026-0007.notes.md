# CCT-2026-0007 — notes

## 2026-06-25 — Code Author — deviation summary

**t4 partially exercised (level2 not run live against staging).**

Two of the three t4 observable sub-criteria were met end-to-end in this session:

- `make verify-spec` exits 0 (34/34 checks pass against the merged
  GET-suitability consumer spec).
- `make level1` provisions pinned oasdiff v1.20.0 to a repo-local `bin/`,
  diffs the consumer spec against the v3-branch provider OpenAPI
  (`data-2026-spec-conformance-v3:mt-landvault-api/docs/openapi.yaml`), emits a
  real changelog, and the breaking-change gate returns PASS (provider is a
  superset; only warnings).

The third sub-criterion — `make level2` run to completion against staging with
its only failures matching the expected-failures manifest — could **not** be
executed: the `STAGING_API_PASSWORD` secret is not present in the Code Author's
environment, so no authenticated request to
`https://landvault-api.staging.miraterrasoil.com` is possible here.

In lieu of the live run, the level2 path was validated structurally:

1. **Wiring smoke test** — `BACKEND_URL`/`BACKEND_AUTH` set to an unreachable
   local URL: the runner launches schemathesis with the `-u` base-URL override
   and the injected Basic `Authorization` header, observes connection errors,
   and exits non-zero (FAIL). This confirms there is no silent-pass path and the
   base-URL override is applied.
2. **Classifier unit check** — the `classify()` function was run against a
   synthetic NDJSON event stream: known-deviation paths
   (`/areal/suitability`, `/areal`) with manifest-listed checks are partitioned
   as EXPECTED (reported, not build-failing), while a `not_a_server_error`
   failure on `/areal/regions` (not in the manifest) is partitioned as
   UNEXPECTED (build-failing). The partition is correct.

The runner itself is complete and correct. Completing t4 requires only running
`BACKEND_URL=... BACKEND_AUTH=... make level2` in an environment that holds the
staging credential — a Manager/Tester step, not further implementation. t4 is
left at `in-progress` to keep that visible; t0–t3, t2a, and t5 are `resolved`.
